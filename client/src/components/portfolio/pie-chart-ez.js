/*
import React, { Component } from 'react';

import { PieChart } from 'react-easy-chart';

export default class PieChartEz extends Component {
    constructor(props) {
        super(props);
    }

    render() {
        const data = [];
        this.props.assets.map((asset) => {
            data.push({
                key: asset.currency,
                value: asset.value
            })
        });

        return (
            <PieChart
                data={data}
             />
        );
    }
}
*/